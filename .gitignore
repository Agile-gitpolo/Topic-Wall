# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ruby/Bundler
/.bundle
/vendor/bundle/

# Rails
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep
/storage/*
!/storage/.keep
/db/*.sqlite3
/db/*.sqlite3-journal
/config/master.key
.env*

# Node/Vite/前端相关（已用CDN可全部忽略）
/node_modules/
/vite-project/
vite.config.*
tailwind.config.*
tsconfig.json
postcss.config.*
package.json
package-lock.json
yarn.lock

# 编辑器/IDE
*.swp
*.swo
*.idea/
*.vscode/
.cursorignore

# 业务脚本/调试
/exit
/t.all.each { |p| puts "#{p.id}: #{p.title} (topic_id: #{p.topic_id})" }
/script/

# GitHub/CI
.github/

# 其他
.DS_Store
*.bak
*.old

# 业务文档（如不想上传）
/Project Technical Architecture and Functional Process.md
/QUICK_START.md

